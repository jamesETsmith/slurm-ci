jobs:
  test-rocm-versions:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rocm_version: [ "6.4.2", "6.4.3" ]
    container:
      image: rocm/dev-ubuntu-24.04:${{ matrix.rocm_version }}-complete
    steps:
      - name: Run tests
        run: echo "Testing on ROCm version ${{ matrix.rocm_version }}"